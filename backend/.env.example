# --- Server ---
PORT=3000
NODE_ENV=development
JWT_SECRET=change_me_to_a_long_random_string
CORS_ORIGINS=http://localhost:4200
AUTH_COOKIE_NAME=focusnow_token
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# --- Database ---
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=focusnow

# --- App URL (used in password reset link) ---
APP_BASE_URL=http://localhost:4200

# --- Email (preferred: Gmail with App Password) ---
# Generate at: https://myaccount.google.com/apppasswords
GMAIL_USER=focusnowid@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password
# Optional custom From header (defaults to "FocusNow <GMAIL_USER>")
# MAIL_FROM=FocusNow <focusnowid@gmail.com>

# --- Alternative SMTP (if not using Gmail) ---
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
