{
  "name": "focus-now-root",
  "version": "0.0.0",
  "private": true,
  "description": "Root wrapper to build Ionic Angular app from frontend/ and sync Capacitor Android for Appflow.",
  "scripts": {
    "build": "npm run build:frontend && npm run cap:sync",
    "build:frontend": "cd frontend && npm ci --no-audit --no-fund && npm run build",
    "cap:ensure": "node scripts/ensure-android.js",
    "cap:sync": "npm run cap:ensure && npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/cli": "7.4.3",
    "@capacitor/core": "7.4.3",
    "@capacitor/android": "7.0.0",
    "typescript": "^5.9.3"
  }
}
